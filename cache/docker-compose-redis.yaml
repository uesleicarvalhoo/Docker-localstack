version: '3'

services:

  redis:
    image: redis
    container_name: redis-single

    ports:
      - 6379:6379

    networks:
      - global-default

    volumes:
      - redis-data:/data

volumes:
  redis-data:
    external: true

networks:
  global-default:
    external: true
